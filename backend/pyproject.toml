[project]
name = "map-feature-demo-backend"
version = "0.1.0"
description = "FastAPI backend and MCP server for the map feature demo"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.141.1",
    "httpx==0.28.1",
    "mcp>=2.0.0",
    "pydantic-settings==2.14.2",
    "pymongo==4.17.0",
    "uvicorn==0.52.1",
]

[tool.uv]
package = false
